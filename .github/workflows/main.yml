on: [ push ]

jobs:
  First-Job:
    runs-on: macos-latest
    name: Github Composite Actions

    steps:
      - uses: actions/checkout@v3

      - id: customerRepo
        uses: rogeruiz22/odoafRepo-friday27-950@master
        with:
          who-to-greet: "Incubator Team !!!!!"

      - run: echo random-number ${{ steps.customerRepo.outputs.random-number }}
        shell: bash