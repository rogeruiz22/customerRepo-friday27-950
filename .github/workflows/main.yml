on: [ push ]

jobs:
  First-Job:
    runs-on: ubuntu-latest
    name: Github Composite Actions

    steps:
      - uses: actions/checkout@v3

      - id: customerRepo
        uses: rogeruiz22/odoafRepo-friday27-950@v3
        with:
          who-to-greet: "Incubator Team !"
          comando: "mvn test"