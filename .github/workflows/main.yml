on: [ push ]

jobs:
  First-Job:
    runs-on: ubuntu-latest
    name: Github Composite Actions

    steps:
      - uses: actions/checkout@v3

      - id: customerRepo
        uses: rogeruiz22/odoafRepo-friday27-950@master
        with:
          who-to-greet: "royer"
          another-input: "mvn test"

      - run: echo output-variable ${{ steps.customerRepo.outputs.output-variable }}
        shell: bash