on: [ push ]

jobs:
  First-Job:
    runs-on: ubuntu-latest
    name: Github Composite Actions

    steps:
      - uses: actions@checkout@v3
      - id: customerRepo
        uses: rogeruiz22/odoafRepo-friday27-950@master
        with:
          comando: 'mvn test'
      - run: ${{ steps.customerRepo.outputs.hovno }}