on: [ push ]

jobs:
  First-Job:
    runs-on: ubuntu-latest
    name: Github Composite Actions

    steps:
      - id: customerRepo
        uses: rogeruiz22/odoafRepo-friday27-950@master
        with:
          comando: "mvn test"